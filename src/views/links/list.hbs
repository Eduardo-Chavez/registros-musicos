<div class="container p-4">
    <div class="row">
        {{#each links}}
        <div class="col-md-3">
            <div class="card text-center">
                <div class="card-body">
                        <h3 class="card-title text-uppercase">
                            {{instrumento}}
                        </h3>
                    <p class="m-2">{{nombre}}</p>
                    <p class="text-muted">
                        {{fecha}}
                    </p>
                    <a class="btn btn-danger" href="/links/delete/{{cveMusico}}">
                        Eliminar
                    </a>
                    <a class="btn btn-secondary" href="/links/edit/{{cveMusico}}">
                        Editar
                    </a>
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>No hay ningun Musico registrado.</p>
                <a href="/links/add">Registrar Musico.</a>
            </div>
        </div>
        {{/each}}
    </div>
</div>